# Stage 23：小学探索手册

状态：`IN_PROGRESS`

产品 Phase：全面教学小学深度

需求：REQ-030、REQ-031、REQ-044、REQ-045、REQ-046

## 目标与非目标

让小学孩子在现有课程事实链上获得真正区别于幼儿园、初中和高中的学习页面：低年级一次读懂并完成一个短活动，高年级能看见“要做/在做/再练”的可解释状态，按“先猜、验证、解释”完成探索，并可随时标记没看懂、暂停向家长求助。

本 Stage 不建设班级、老师端、排名、连续签到、倒计时惩罚或基于同龄人的能力预测；不把年龄当能力诊断。首个纵切不新增服务端年级字段，也不把本机“没看懂”提示冒充持久证据；服务端年级带、求助记录、错因和间隔再练必须在后续工作包另行落地并验收。

## 年级带与安全不变量

- `LOWER_PRIMARY`：通常 6–8 岁，对应一至三年级；一次突出一个活动，使用“读懂 → 试一试 → 说发现”。
- `UPPER_PRIMARY`：通常 9–11 岁，对应四至六年级；显示要做/在做/再练事实和当前探索，但不展示同龄比较。
- 首个 Android 纵切根据家长配置的出生日期计算展示带；解析失败时退回信息更少的低年级视图。该映射只是界面支架，后续服务端年级配置优先于年龄推断。
- 活动完成仍只认 Stage 21 的 `VIEWED/ATTEMPTED/CHECKED/PARENT_CONFIRMED`；“我没看懂”本轮只展开求助建议，不提交 Attempt、不绕过答题或制造完成证据。
- 视频不自动播放、不自动下一集，只按实际播放阈值记录 `VIEWED`；错题使用温和提示和再次尝试，不写“失败”。

## 两遍视觉与交互规格

第一遍采用“可翻的探索夹页”：作业纸白 `#FBFCF7`、铅笔墨 `#263A45`、湖蓝 `#3C7891`、格线蓝 `#CFE0E7`、批注橙 `#D98245`；系统无衬线以字号、字重和小型等宽状态字区分角色。低年级为单张当前页，高年级为三格事实条加当前页；夹页左侧色签是唯一签名元素。

第二遍删除通用仪表盘百分比、横向课程轮播、多张同权卡片和装饰性印章。低年级保留一个主要活动；高年级只增加计划事实和求助出口，不增加奖励数字、排名或持续动效。窄屏内容纵向排列，平板横屏保留清楚留白；Reduced Motion 下无依赖位移的反馈。

```text
低年级：[今天] [学习] [我的]
        ┌ 当前探索夹页 ────────────┐
        │ 读懂 → 试一试 → 说发现  │
        │ 现在只做：……             │
        │        [开始探索]         │
        └───────────────────────────┘

高年级：[今天] [学习] [我的]
        [要做 2] [在做 1] [再练 0]
        ┌ 当前探索：先猜 → 验证 → 解释 ┐
        │ [继续当前一步] [我没看懂]     │
        └───────────────────────────────┘
```

## 工作包

| ID | 状态 | 内容 |
| --- | --- | --- |
| WP23-1 | 已完成 | Stage Spec、REQ-044、两遍设计、路线图/TODO/current/BLOCKERS 与证据骨架 |
| WP23-2 | 离线完成 | Android 低/高年级展示带策略、探索夹页、单一当前活动、事实状态与非完成型求助出口 |
| WP23-3 | 已完成 | 服务端年级带事实、家长受保护配置/审计和历史课程兼容；不得删除旧 Attempt |
| WP23-4 | 待开始 | 语文/数学/英语/科学首批原创活动模板、阅读/练习/现实实验和权利审计 |
| WP23-5 | 待开始 | 错因、没看懂、再次尝试和间隔再练证据；不以最终分数覆盖过程 |
| WP23-6 | 待开始 | 小学家长报告、PostgreSQL、Android 双变体和目标平板验收 |
| WP23-7 | 进行中 | 全量门禁通过后以稳定证书发布 v0.3.6/12；复验公开资产摘要、包名、版本与证书，真机升级独立保留 |
| WP23-8 | 可离线首切 | Android 四学段各 3 个适龄表达的原创短视频活动、当前学段家庭课程入口、家长固定奖励配置、90% 播放后待审核任务与本机账本；服务端自动选课/结算仍待建设 |

## WP23-3/7 实施契约

- 在 `child_experience_profile` 增加可空 `primary_band_override`，只允许 `LOWER_PRIMARY / UPPER_PRIMARY`；在 `child_experience_audit` 同时记录切换前后值。Flyway 只新增 V13，不修改历史迁移。
- `ExperienceProfile` 在有效学段为 `PRIMARY` 时返回 `recommendedPrimaryBand / primaryBandOverride / effectivePrimaryBand`；非小学学段三者均为空。推荐值按出生日期年龄提供界面支架，家长覆盖优先。
- 只有已鉴权且拥有家庭对象权限的家长可写；覆盖值仅能用于小学有效学段，更新继续使用期望版本，并以审计理由保存。切换学段时可显式清空小学覆盖。
- 更新操作只写体验配置与审计，不级联删除或重写 Stage 21 的 CourseVersion、Assignment、ActivityAttempt、Completion 或 MasteryEvidence。
- Android 优先采用服务端 `effectivePrimaryBand`，旧服务端缺字段时才按出生日期安全回退；家长配置界面提供“按年龄建议 / 一至三年级 / 四至六年级”，孩子端不能修改。
- v0.3.6 使用 `versionCode=12`、既有 applicationId 与稳定 release 证书，通过 tag workflow 生成唯一 APK；不把本地构建或 GitHub 页面存在当作发布完成。
- 服务端家庭课程只投影已发布且与 `effectiveStage` 匹配的版本；Android 另提供明确标为本机的 12 个原创短视频活动。孩子不能浏览草稿、其他学段、答案键、来源 URL 或自行修改学段。学段切换只改变目录投影，不删除旧课程、Assignment、Attempt 或既有本机幼儿园播放记录。
- 家长可为自主课程配置固定 `Money/Coin/XP` 奖励，确认前清楚显示数额；本机升级兼容默认保持 `0 Money / 2 Coin / 5 XP`，未来服务端规则默认必须为零并由家长显式配置。视频活动必须沿用实际播放阈值形成 `VIEWED`，全部必需证据满足后才能完成课程并结算一次；客户端重复、网络重放和并发不得重复发奖。
- Money/Coin 变化必须与不可变 LedgerEntry 在同一事务发生；不接真实支付，不使用随机奖励、连续签到、倒计时或以延长观看时长为目标的刺激。

## 验证方式

| ID | 环境 | 操作 | 预期 |
| --- | --- | --- | --- |
| V23-01 | Android JVM | 输入 6/8/9/11 岁及非法出生日期 | 6–8 为低年级、9–11 为高年级；非法值安全回到低年级 |
| V23-02 | Android JVM/UI 编译 | 输入待做、进行、答错、返工、完成课程 | 状态只来自真实 Assignment/Activity；求助不产生完成证据 |
| V23-03 | Android debug/release | 单测、lint、assemble | 两变体通过，无新增儿童一级导航，无小学页面奖励/排名/倒计时 |
| V23-04 | H2/PostgreSQL | 更新小学年级带、读取配置/审计，并在切换前后读取既有 CourseVersion/Attempt | 家长权限、乐观锁、枚举约束和审计通过，历史课程与 Attempt 数量/标识不变；求助/错因/再练仍归 WP23-5 |
| V23-05 | 目标平板 | 横竖屏、TalkBack、大字体、Reduced Motion、低/高年级完整活动 | 当前行动明确，求助可退出，内容不截断且历史证据不丢失 |
| V23-06 | 治理 | diff、Markdown、JSON、OpenAPI | 文档与契约一致，无敏感信息 |
| V23-07 | GitHub Release | 推送 main 与 v0.3.6 tag，等待 workflow，下载公开 APK 并检查 | workflow 成功；asset SHA-256 与公开 digest 一致；包名、versionName/versionCode 和稳定证书均匹配 |
| V23-08 | H2/PostgreSQL/API | 家长改学段、配置奖励；孩子查询目录、认真看完、重复/并发结算 | 目录立即按有效学段变化；非匹配课程/草稿/孩子写配置拒绝；未达观看阈值不完成；达标后只发一次固定奖励并同时写 LedgerEntry |
| V23-09 | Android JVM/两变体 | 家长保存学段/奖励，孩子刷新自主课程、播放并同步完成 | 页面直接展示匹配课程和明确奖励，不显示答案/开放网页；离线、401、409 保留可恢复事实，不虚假显示已入账 |

## 完成标准

- [x] AC23-01：`PASS` — 目标、边界、安全不变量、两遍设计、WP/V/AC 在实现前固化，并登记 REQ-044。
- [x] AC23-02：`PASS_OFFLINE` — 小学动态课程不再复用初高中通用路径：低年级显示“读懂/试一试/说发现”和唯一当前页，高年级显示由 Assignment/Activity 推导的要做/在做/再练事实；“我没看懂”只改变本地帮助展开状态，不调用 ViewModel 或写 Attempt。Android 两变体各 47 项、lint/assemble 通过；目标平板归 AC23-06。
- [x] AC23-03：`PASS` — V13 持久保存小学分段覆盖及切换前后审计；家长权限、孩子写入 403、跨家庭 404、版本冲突 409、非小学分段 400 通过；H2/PostgreSQL 16.15 均证明既有 Attempt 保留。
- [ ] AC23-04：首批主要学科原创内容有版本、目标、时长、权利和撤回记录。
- [ ] AC23-05：错因、求助、复做和间隔复习形成服务端最小证据链，答案与孩子投影隔离。
- [ ] AC23-06：`PARTIAL` — H2 全量、PostgreSQL 16.15 定向、Android debug/release JVM/lint/assemble 与文档契约门禁通过；目标平板仍 `BLOCKED`。
- [ ] AC23-07：v0.3.6/12 由稳定证书签名并成为 GitHub latest，公开资产完成摘要、包名、版本和证书复验；目标平板覆盖升级不因此标记通过。
- [ ] AC23-08：`PARTIAL` — 服务端学段投影已具备，Android 本机课程/奖励链已通过；服务端自主课程自动选课、预设奖励事务结算、幂等/并发账本门禁尚未实现。
- [ ] AC23-09：`PASS_OFFLINE` — Android 家长小学分段、四学段各 3 个课程投影、服务端家庭课程入口、固定奖励配置、90% 播放阈值、一次待审核任务和审核后本机 Ledger 通过两变体门禁；目标平板仍 `BLOCKED`。

## 当前限制与交接

Stage 22 的目标平板阻塞继续独立存在。Stage 23 WP23-1–3 已完成，WP23-8 完成 Android 可离线首切；仍不能宣称已有足量小学学科包、持久求助/错题本、小学报告或服务端自主学习自动奖励。下一工程顺位是 REQ-046 的服务端自动选课/奖励账本闭环，随后回到 WP23-4/5。
