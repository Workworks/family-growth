# Family Growth — Agent 启动协议

本仓库所有 Coding Agent 的入口。**详细工程规范在 [docs/codex-skills.md](docs/codex-skills.md)，本文件不重复其全部内容。**

不得依赖上一次会话的记忆代替读取。用户新增长期要求时，必须在当轮同步维护本文件或 `docs/codex-skills.md`，并追加登记到需求账本。每次对话，无论是否发生代码修改，都必须按本协议从当前仓库事实源启动。

## 最高产品行为准则：儿童最佳利益优先

儿童端从 3 岁开始提供直接操作体验。任何需求、设计、排期和实现发生冲突时，**儿童的安全、身心健康、理解能力与现实世界成长优先于活跃、留存、在线时长、奖励强度、金融功能完整度和商业目标**。该准则是产品行为层最高优先级，持续适用于所有 Stage；不得被普通 P0/P1 功能需求覆盖。

1. 儿童端一次只突出一个主要行动，一级导航最多 3 个；语言短、具体、可朗读，触控目标大且结果可预测。
2. 3–5 岁默认亲子共同使用。App 应把孩子引向现实中的阅读、运动、整理和亲子互动，完成后回到家长回应，不以延长屏幕使用为成功指标。
3. 不使用连续签到、排行榜、倒计时施压、随机奖励、抽奖、宝箱、损失厌恶、羞辱、无限滚动、自动播放、强提醒或其他利用儿童心理弱点的成瘾/操纵设计。
4. 反馈强调行动、努力和下一步，不给孩子贴“聪明/笨/失败”等人格标签；错误和休息提示保持中性、温和、可恢复。
5. Money/Coin、兑换、基金、NAV、费用、流水等复杂财商概念按年龄渐进展示。3–5 岁只显示“完成、星星、存起来、请家长一起”等具体概念，交易、基金和费率由家长端托底。
6. 默认高隐私、最少数据、低刺激和 Reduced Motion；不得为提高参与度牺牲隐私或健康。
7. 儿童页面、教学内容、交互密度和反馈强度必须按幼儿园、小学、初中、高中四个学段分别设计；不得用同一套页面只换标题或颜色冒充适龄分层。
8. 家长可配置孩子年龄，由服务端计算推荐学段，并可在家长 PIN/RBAC 保护下覆盖学段；孩子不能自行修改。学段变更必须保留审计且不得删除历史学习记录。
9. 幼儿园点击反馈可以明显、快乐，但必须短促、可预测、可关闭并尊重系统触觉与 Reduced Motion；禁止持续震动、闪烁、随机庆祝、反复弹跳或用强刺激延长使用。小学、初中、高中随年龄逐级降低装饰性动效并提高信息自主性。
10. 纪录片和外部教学视频只能进入家长审核的适龄内容目录；离线内置须为原创或具有明确授权的完整内容，官方外链须在家长控制下打开。儿童端不得进入开放网页、广告、评论、推荐流或自动播放下一集。

详细验收基线见 [儿童端发展与行为设计基线](docs/design/36-child-experience-behavioral-baseline.md)和[四学段教学体验与内容分层](docs/design/39-age-stage-teaching-experience.md)。

## 项目定位

Family Growth 是供单个家庭长期使用的儿童成长培养与家庭财商教育 Android App。V1 由一个 Android APK 和一个 Spring Boot 模块化单体后端组成。金额是家庭内部教育奖励账本，投资是纯模拟；不得宣称或接入真实银行、支付、证券、基金交易或投资推荐。

## 每次任务的执行顺序

### Step 1 加载规则

完整读取 [docs/codex-skills.md](docs/codex-skills.md)，确认事实源、Token、Spec、测试、Git、证据、文档和安全策略。

### Step 2 检查当前阶段、任务账本与 Spec

依次读取 [docs/stage-current.md](docs/stage-current.md) 与 [docs/TODO.md](docs/TODO.md)。前者是当前 Stage 上下文和重点快照，后者是按优先级、推进责任和下一动作组织的执行账本；两者不一致时，以对应 Stage 报告为准并在本次任务中同步修正。

读取 [Spec 驱动开发指南](docs/development/33-spec-driven-development.md)。新增功能、跨模块修复、界面重构、发布、部署或治理结构变化，**实现前必须确认目标 Stage 报告或验收规格已经写清目标、边界、验证方式和完成标准**；缺任一项先补 Spec。只读咨询和不改变行为的机械修改可不新建 Spec。

用户提出的每条新需求、补充、约束、优先级、否定意见或长期标准，必须在当轮追加到 [需求账本](docs/requirements/requirement-ledger.md)，保留原意、优先级、状态、目标 Stage 和落地证据；不得只留在对话记忆中，也不得删除历史记录。

### Step 3 检查已知问题

执行 `git status --short`，识别用户已有修改和未跟踪文件，**不得覆盖、丢弃或提交不属于本任务的工作**。

读取 [docs/bug/bugList.md](docs/bug/bugList.md)、[docs/aq/aq.md](docs/aq/aq.md) 与 [docs/BLOCKERS.md](docs/BLOCKERS.md)。相关未完成项必须在当前任务处理或说明；无关项继续保留，不得静默删除。参考仓库的规范入口是 `docs/BLOCKERS.md`，本项目不建立冲突的 `BLOCK.md` 第二事实源。

### Step 4 执行任务

按 Spec 的编号工作包和完成标准实施。坚持最小修改、最少必要读取和与风险匹配的测试范围。发现需要扩大边界时先更新 Spec；若实质扩大用户授权、涉及外部写入或破坏性操作，停止并请求确认。

关键范围、架构、环境、实现、验证和状态行为同步写入 [项目行为日志](docs/governance/activity-log.md)；可回放命令和真实结果写入 `docs/evidence/stage-N/`。判断完成度看实际代码、迁移、构建和真实测试，不只看页面、接口或报告。

### Step 5 完成检查

执行 `git diff`、`git diff --check`，确认修改符合需求、没有无关变更和敏感信息。按 `docs/codex-skills.md` 的测试矩阵执行门禁，并运行文档本地链接检查、结构化证据 JSON 解析和 OpenAPI 解析。

逐条回填 Spec 的完成标准与证据；未执行、未通过或缺真实环境的条目不得勾选。同步维护 `docs/TODO.md`、`docs/stage-current.md`、`docs/BLOCKERS.md`、Stage 路线图和 Stage 索引；完成项从 TODO 移除，历史结论留在 Stage 报告和证据中。

### Step 6 输出报告

```text
完成:
修改:
验证:
下一步:
```

只读咨询可不修改代码，但仍需完成 Step 1–3，并在结果中区分已完成、未完成和外部阻塞。报告不得把“理论可行”“代码已写”或 Mock 结果当作真实验收。

## 长期需求与建议登记

1. 需求账本是跨会话长期事实源，状态只使用 `NEW / ACCEPTED / PLANNED / IN_PROGRESS / VALIDATED / DEFERRED / REJECTED / SUPERSEDED`。
2. 优先级只使用 `P0 / P1 / P2 / P3`；P0 为安全、账本不变量、治理协议或当前交付阻断。
3. 需求变化时新增记录并关联旧记录；历史需求不得删除或静默改写。
4. `TODO.md` 只承载未完成且可继续执行的事项，不能替代需求历史；`BLOCKERS.md` 单独记录解除条件和责任方。
5. 影响长期执行方式的用户要求必须同步固化到本文件或 `docs/codex-skills.md`。

## 不可违反的红线

1. **不虚假标记完成。** 未真实运行验证不得标记 `COMPLETED`；缺外部环境、仓库、凭据或设备使用 `BLOCKED` 并留证据。
2. **不伪造产品能力。** 固定成功、Mock、空页面、构建成功不能冒充登录、钱包、基金、防沉迷、更新或端到端能力。
3. **账本不可绕过。** Money、Coin 的任何变化必须在同一事务追加 LedgerEntry；金额使用 `BigDecimal` 和数据库定点数。
4. **费用必须透明。** 兑换、基金和线下兑现的手续费、价差与预计到账必须在确认前展示并进入流水，禁止隐藏扣费。
5. **认证授权由服务端保证。** 家长关键配置、审核、调账和数据导出必须鉴权；孩子不得绕过 PIN、RBAC 或家庭/孩子对象权限。
6. **儿童隐私最小化。** PIN/密码强哈希；儿童资料、图片、使用记录最小采集、最小展示、可导出删除；日志不得泄露敏感字段。
7. **不接真实金融。** 虚拟钱包和基金仅用于家庭教育，不接真实银行、支付、证券、基金代码或行情，不暗示保本收益。
8. **secret 不出现在** Git、日志、证据、Release 附件、审计载荷或回复中。
9. **不破坏用户工作树。** 禁止 `git reset --hard`、强制覆盖、丢弃用户改动、重写共享历史或擅自清理用户设备/AVD。
10. **测试不做真实破坏。** 不执行真实递归删除、force push、`DROP DATABASE`、停系统服务或连接生产库。
11. **Android 安装边界不可绕过。** APK 更新必须校验来源、版本、大小、SHA-256、包名与签名，并交由 Android 系统让用户确认。
12. **需求不得丢失。** 每轮新需求和建议必须在本轮结束前落入文档并回链 Stage/证据。
